d = input('Введите дату в формате ДД.ММ.ГГГГ: ')
def f(d):
    day, month, year = map(int, d.split('.'))
    if day * month == year % 100:
        return True
    else:
        return False
print(f(d))
